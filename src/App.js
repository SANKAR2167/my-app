import './App.css';
import { MovieList } from './MovieList';
// app component = view + logic (declaration)
function App() {
  return (
    <div className="App">
      <MovieList/>
    </div>
  );
}

export default App;
